CheckAuthResponse:
  description: 認証チェック
  content:
    application/json:
      schema:
        $ref: "../schemas/Auth.yml#/Auth"
      examples:
        example-1:
          $ref: "../schemas/Auth.yml#/AuthExample1"
